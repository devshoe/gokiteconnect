// Package nse calls the data functions used by NSE India equity pages.
//
// NSE's browser pages are backed by /api/NextApi endpoints that commonly
// require a page visit first so the edge layer can set cookies.
package nse
